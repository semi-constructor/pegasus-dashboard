feat: comprehensive redesign, security hardening, and structural enhancements

This commit encompasses several major initiatives across the Pegasus Dashboard application:

1. **Cinematic Landing Page Redesign**:
   - Replaced the basic landing page with a dynamic, immersive, 3D-heavy experience.
   - Introduced `CinematicHero`, `FeatureScroll`, and `EcosystemIntegration` components using Framer Motion, Anime.js, and Three.js.
   - Transitioned from displaying raw commands to showcasing feature descriptions and benefits in a scroll-driven UI.
   - Improved `AnimatedNavBar` and `BackgroundScene` with modern aesthetic micro-interactions and transitions.

2. **Security Hardening**:
   - Comprehensive security hardening pass applying secure coding best practices.
   - Improved API validation logic and error handling mechanisms to prevent data leakage.
   - Enhanced authentication workflows and session management in `src/auth.ts` and `src/lib/auth-utils.ts`.
   - Updated Zod schemas (`schemas/auth.ts`, `schemas/automod.ts`, `schemas/economy.ts`, etc.) to enforce stricter data validation constraints.

3. **New Features & Pages**:
   - Added robust Pricing (`src/app/pricing`, `src/components/pricing`) and Changelog (`src/app/changelog`, `src/components/changelog`) pages.
   - Developed `LiveTelemetryDashboard` for real-time bot performance insights.

4. **Codebase Integrity & Audit Improvements**:
   - Restructured server actions in `src/app/actions.ts` and `src/app/admin/actions.ts` for safer execution.
   - Improved documentation and `API_DOC.md` consistency.
   - Cleaned up dependencies in `package.json` and `package-lock.json` to support new animation and UI libraries securely.
